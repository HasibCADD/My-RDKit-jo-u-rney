simple but important

# 1. Create molecules (Lesson 1)
aspirin = Chem.MolFromSmiles('CC(=O)Oc1ccccc1C(=O)O')
paracetamol = Chem.MolFromSmiles('CC(=O)Nc1ccc(O)cc1')
caffeine = Chem.MolFromSmiles('CN1C=NC2=C1C(=O)N(C(=O)N2C)C')

# Add explicit hydrogens (Lesson 2)
mols = [Chem.AddHs(m) for m in [aspirin, paracetamol, caffeine]]

# Names for grid (using _Name property - Lesson 1)
names = ["Aspirin", "Paracetamol", "Caffeine"]
for mol, name in zip(mols, names):
    mol.SetProp("_Name", name)

# 2. Visualize grid
Draw.MolsToGridImage(mols, legends=names, molsPerRow=3, subImgSize=(400, 300), useSVG=True)

# 3. Count aromatic atoms (Lesson 2 + 3 atom iteration)
print("Aromatic atom counts:")
for name, mol in zip(names, mols):
    arom_count = sum(1 for atom in mol.GetAtoms() if atom.GetIsAromatic())
    print(f"{name}: {arom_count} aromatic atoms")

# Bonus: "Edit" caffeine → theophylline (remove one methyl group - real drug relative)
# Direct SMILES way (reliable from Lesson 3)
theophylline_smiles = 'CN1C=NC2=C1C(=O)NC(=O)N2C'  # Theophylline
theophylline = Chem.AddHs(Chem.MolFromSmiles(theophylline_smiles))
theophylline.SetProp("_Name", "Theophylline (caffeine relative)")

# Grid: original caffeine vs modified
Draw.MolsToGridImage([mols[2], theophylline], 
                     legends=["Caffeine", "Theophylline (demethylated)"],
                     molsPerRow=2, subImgSize=(450, 350))





the simplest way to grid for multiple compounds

# 1. Create molecules (Lesson 1)
aspirin = Chem.MolFromSmiles('CC(=O)Oc1ccccc1C(=O)O')
paracetamol = Chem.MolFromSmiles('CC(=O)Nc1ccc(O)cc1')
caffeine = Chem.MolFromSmiles('CN1C=NC2=C1C(=O)N(C(=O)N2C)C')

# Add explicit hydrogens (Lesson 2)
mols = [Chem.AddHs(m) for m in [aspirin, paracetamol, caffeine]]

# Names for grid (using _Name property - Lesson 1)
names = ["Aspirin", "Paracetamol", "Caffeine"]
for mol, name in zip(mols, names):
    mol.SetProp("_Name", name)

# 2. Visualize grid
Draw.MolsToGridImage(mols, legends=names, molsPerRow=3, subImgSize=(400, 300), useSVG=True)

